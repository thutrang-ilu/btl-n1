@model IEnumerable<BTLN1.Models.Luong>

@{
    ViewData["Title"] = "Index";
}

<h1>DANH SÁCH MỨC LƯƠNG</h1>

<p>
    <a asp-action="Create">Tạo mức lương</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SoLuong)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SoLuong)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LuongID">Cập nhật</a> |
                <a asp-action="Details" asp-route-id="@item.LuongID">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.LuongID">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
